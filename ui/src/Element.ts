type Element = {
    symbol: string;
    name: string;
    atomicNumber: string;
    atomicWeight: string;
    electronicConfiguration: string;
    period: number;
    group: number;
};

export default Element;