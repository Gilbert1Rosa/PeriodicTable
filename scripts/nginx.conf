server {
        listen 80;
        listen [::]:80;

        root /var/www/periodic-table/html;
        index index.html index.htm;

        server_name _;

        location / {
                try_files $uri $uri/ =404;
        }
}
